name: Deploy with Terraform

on:
  push:
    branches: [ "main" ]
  pull_request:

jobs:
  deploy:
    uses: souzapablo/github-actions/.github/workflows/terraform.yml@v1
    with:
      environment: production
    secrets:
      TF_API_TOKEN: ${{ secrets.TF_API_TOKEN }}
